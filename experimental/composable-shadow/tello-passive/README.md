# Local passive Tello service observation

Start with [the complete guide](../../../docs/tello-passive-observation.md).
The observer records a copy after an existing client call returns. It neither
sends a Tello command nor makes a decision about whether that command may run.
This is an experimental observation entry point, not a flight-control layer.
