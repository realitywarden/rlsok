--- a/src/soft_propulsors_control/soft_propulsors_control/controller.py
+++ b/src/soft_propulsors_control/soft_propulsors_control/controller.py
@@ -1453,15 +1453,39 @@
 
         servo_ids = list(targets.keys()) if only_named else self.all_ids
 
+        # Validate the whole batch before smoothing, clamping or publishing.
+        # Invalid numeric input must never become a finite end-stop command.
+        try:
+            if len(set(servo_ids)) != len(servo_ids):
+                raise ValueError('duplicate servo IDs')
+            if any(sid not in self.all_ids for sid in targets):
+                raise ValueError('unknown target servo')
+            for sid in servo_ids:
+                raw = targets.get(sid, 0.0)
+                if isinstance(raw, bool) or not isinstance(raw, (int, float)) or not math.isfinite(raw):
+                    raise ValueError('non-finite or non-numeric target')
+                if mode_code == MODE_POSITION:
+                    bounds = self.limits.get(sid)
+                    if (bounds is None or len(bounds) != 2
+                            or not all(math.isfinite(x) for x in bounds)
+                            or bounds[0] > bounds[1]):
+                        raise ValueError('missing or invalid position limits')
+                    a = self.command_smoothing
+                    prev = self._cmd_smooth.get(sid)
+                    if not math.isfinite(a) or not 0.0 <= a <= 1.0:
+                        raise ValueError('invalid smoothing coefficient')
+                    if a > 0.0 and prev is not None and not math.isfinite(prev):
+                        raise ValueError('non-finite smoothing history')
+        except (TypeError, ValueError, OverflowError) as exc:
+            self.get_logger().error(f"Command batch rejected: {exc}")
+            return
+
         ids, modes, values = [], [], []
         for sid in servo_ids:
             raw = targets.get(sid, 0.0)
             if mode_code == MODE_POSITION:
-                # Position targets are relative to the servo's homed zero.
-                # Limits are ±inf for now (clamping removed), so this passes
-                # the target through unchanged; restore finite PITCH_LIMIT/
-                # HEAVE_LIMIT to re-enable the clamp.
-                lo, hi = self.limits.get(sid, (-math.inf, math.inf))
+                # Finite role limits remain relative to the reviewed homed zero.
+                lo, hi = self.limits[sid]
                 val = max(lo, min(hi, raw))
                 # Output low-pass: ease each command toward its target so the
                 # high-frequency jitter in the setpoint stream (encoder/IMU
--- a/src/soft_propulsors_control/soft_propulsors_control/Dynamixel_XW430_T200_interface.py
+++ b/src/soft_propulsors_control/soft_propulsors_control/Dynamixel_XW430_T200_interface.py
@@ -371,15 +371,28 @@
     def _cmd_cb(self, msg: Float32MultiArray):
         try:
             data = msg.data
+            if not data or len(data) % 3:
+                raise ValueError('expected equally sized ID, mode and value arrays')
+            if not all(math.isfinite(x) for x in data):
+                raise ValueError('non-finite joint command')
             n = len(data) // 3
-            if n == 0:
-                return
+            ids, modes = list(data[:n]), list(data[n:2*n])
+            if any(x != int(x) or not 0 <= x <= 252 for x in ids):
+                raise ValueError('invalid unicast servo ID')
+            if len(set(ids)) != n:
+                raise ValueError('duplicate servo ID')
+            if any(x not in (1, 3) for x in modes):
+                raise ValueError('unsupported command mode')
+            if self.is_configured and any(int(x) not in self.active_ids for x in ids):
+                raise ValueError('servo is not configured')
             self.latest_command = (
-                [int(round(data[i])) for i in range(n)],
-                [int(round(data[n + i])) for i in range(n)],
-                [float(data[2 * n + i]) for i in range(n)],
+                [int(x) for x in ids], [int(x) for x in modes],
+                [float(x) for x in data[2*n:]],
             )
-        except Exception as e:
+        except (TypeError, ValueError, OverflowError) as e:
+            # Prevent a rejected frame from leaving a pending software replay.
+            # This does not cancel a command already received by a servo.
+            self.latest_command = None
             self.get_logger().warn(f"Invalid joint command: {e}")
 
     def _hardware_loop(self):
