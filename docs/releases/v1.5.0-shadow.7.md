# v1.5.0-shadow.7 — afternoon technical feedback review

This prerelease adds the [10 September afternoon review guide](../email-feedback-afternoon-20260910.md), a local `profile compare-nav2` command, seven reproducible synthetic CLI examples, and confirmed simulation launch references in generated hexapod source workspaces. It retains the earlier controller workflows and patched dependencies from v1.5.0-shadow.6.

The Nav2 comparison separates selected smoother semantics, CLOSED_LOOP odometry provenance, supplied command-path topology, loaded plugin identities and the exact resolved per-goal selectors. Missing/ambiguous facts report `INCOMPLETE`; changed selected facts report `REVIEW_REQUIRED`. It creates no approval and provides no Nav2 execution transport. Matching local inputs do not establish a physical envelope or authenticated ROS graph.

The guide also delivers a focused EAL parser/rule review with reproducible multiple-controller and non-finite-value findings, explains launch maintenance and motor-service boundaries, and points to the existing Elite/SVH/runtime provenance implementations. It does not claim new customer-specific vendor drivers.

Targeted verification performed:

- Five new Nav2 unit-test groups passed, including same-limit semantic changes, source binding, topology ambiguity, selector substitution, volatile-only controls and malformed input.
- Five existing source-workspace tests passed after the launch-reference generation change.
- Seven local CLI cases produced their expected status/exit code and recorded zero hardware dispatch. The first attempt used the older local `dist` and rejected the new command; after compiling the changed source, all seven passed. This was a stale local build, not a customer runtime result.
- Three direct EAL parser/rule probes ran against commit `9701f183169ef024d908825d019c439186edc2ac`; findings and input construction are committed. No EAL full suite or CLI policy-filter validation was run.
- TypeScript compiled for the local CLI delivery check. Clean-source packaging performs its own required build without tests.

The existing Elite/SVH tests were inspected and their earlier results were retained; they were not rerun. No full suite, GitHub Actions, physical robot execution, fresh ROS/Gazebo experiment, Humble validation or installed-bundle acceptance was performed for this version. Earlier scoped experiment evidence remains as documented in v.5/v.6.

Stable v1.4.5 remains separate. This is a local zero-dispatch evaluation prerelease. Publication, website deployment and email delivery do not establish customer use or acceptance.
