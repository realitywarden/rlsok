# v1.5.0-shadow.6 — patched YAML dependency for feedback evaluation

Use this patch prerelease for the [2026-09-10 feedback workflows](../email-feedback-evaluation-20260910.md). It contains all controller tools, source workspaces, propulsor patch and evidence from [v1.5.0-shadow.5](v1.5.0-shadow.5.md), with `js-yaml` updated from 4.3.1 to 4.3.2.

The deployment audit identified [GHSA-2883-xcg3-v3hh](https://github.com/advisories/GHSA-2883-xcg3-v3hh): empty YAML merge sources could consume excessive CPU despite the merge-key budget. This matters to the new local SO-101 configuration-copy reader. The update keeps the existing 2 MiB input bound and controller/gripper validation. This is delivery maintenance, not an additional customer product requirement.

Verification: the two affected controller-change TypeScript tests and five source-workspace tests passed with js-yaml 4.3.2. The production dependency audit reported zero advisories at check time. The clean-source packaging build compiles without running the full test suite or GitHub Actions.

The two successful GenericSystem/ROS Jazzy experiments, six exporter checks, thirty upstream callback checks and synthetic calibration example documented in v.5 remain the evidence for the unchanged feature code. They were not all rerun for this dependency-only patch. The existing raw experiment records and limits remain in this release; there is no new customer, physical robot, Humble or installed-bundle acceptance claim.

The earlier prerelease remains immutable. Stable v1.4.5 stays separate; install v1.5.0-shadow.6 into a fresh evaluation directory. Publication is not permission to move a robot or proof of customer compatibility.
